# Base URL of the backend API. Vite inlines VITE_* values at build time, so in
# Easypanel this must be set as a build argument, not a service env var.
# Leave unset for local dev to fall back to http://localhost:5000.
VITE_API_URL=
